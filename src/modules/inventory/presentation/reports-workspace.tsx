"use client";
import { inventoryProducts, sampleInitialMovements } from "@/modules/inventory";
export function ReportsWorkspace(){function exportCsv(){const rows=inventoryProducts.map(p=>[p.sku,p.name,p.category,p.location,p.available,p.unit,p.unitCost??0]);const csv="SKU;Artículo;Categoría;Ubicación;Existencia;Unidad;Costo unitario\n"+rows.map(r=>r.join(";")).join("\n");const a=document.createElement("a");a.href=URL.createObjectURL(new Blob(["\ufeff"+csv],{type:"text/csv"}));a.download="RFC_inventario.csv";a.click()} return <main className="dashboard-content"><section className="dashboard-heading"><div><p>Dirección · RFC Enterprise</p><h1>Informes</h1><small>Datos de inventario, movimientos, proyectos y operación.</small></div></section><section className="dashboard-panel"><div className="panel-title"><div><p>Exportación</p><h2>Inventario actual</h2></div><div className="row-actions"><button className="inventory-action" onClick={exportCsv}>Exportar Excel (CSV)</button><button className="btn-row-action" onClick={()=>window.print()}>PDF / Imprimir</button></div></div><p className="panel-intro">{inventoryProducts.length} artículos · {sampleInitialMovements.length} movimientos disponibles para análisis.</p></section></main>}
